<h1> Hello Toto</h1>
<ul>
<% @articles.each do |article| %>
<li><%= link_to article.title, article_path(article) %> <%= link_to "delete it", article_path(article), method: :delete, data: { confirm: "Are you sure"} %>
</li>
<% end %>
</ul>
<h3><%= link_to 'Create a new article', new_article_path() %></h3>
